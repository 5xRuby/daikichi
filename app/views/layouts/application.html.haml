!!! 5
%html
  %head
    %title= page_title
    = csrf_meta_tags
    = stylesheet_link_tag "application", "pages/#{controller_name}", "data-turbolinks-track": "reload", media: "all"
    = javascript_include_tag "application", "pages/#{controller_name}", "data-turbolinks-track": "reload"

  %body
    #nav
      = render "shared/navigation"
    #wrap
      = render "shared/flash"
      .container
        = yield
    = render "shared/footer"

= debug params if Rails.env == "development"
